export function operatorResolve(string) {
    switch (string) {
        case 'contains':
            return;
        case 'equals':
        case 'startsWith':
        case 'endsWith':
        case 'isNotEmpty':
        case 'isEmpty':
    }
}
